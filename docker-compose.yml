# Entry point at the repo root so `docker compose up -d` works as the course requires.
# The actual configuration (trainer-controlled rcssserver + rl-agent) lives in docker/.
include:
  - docker/docker-compose.yml
